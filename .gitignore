# --- Dependências do Node ---
node_modules/
npm-debug.log
yarn-error.log
pnpm-lock.yaml

# --- Build de Produção (TypeScript compilado) ---
dist/
build/

# --- Variáveis de Ambiente e Segredos (CRÍTICO) ---
.env
.env.local
.env.*.local

# --- Banco de Dados SQLite (Não queremos versionar seus dados locais) ---
*.db
*.db-journal
*.db-wal
/prisma/*.db
/prisma/*.db-journal
/prisma/data/

# --- Pastas do Sistema de Mangás (GIGANTES - Nunca enviar para o Git) ---
# Pasta onde explodimos os ZIPs/Rars temporariamente
temp/

# Pasta onde ficam os CBZs otimizados finais
library_data/

# Caso você tenha mapeado a pasta de scan dentro do projeto
library_scan/

# --- Arquivos de Sistema Operacional ---
.DS_Store
Thumbs.db

# --- Logs ---
logs/
*.log

# --- IDEs ---
.vscode/
.idea/

# --- Docker (Se tiver volumes locais mapeados aqui) ---
docker-data/
